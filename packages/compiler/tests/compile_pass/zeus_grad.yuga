// Linear gradient draw op (kind 11). Headless dumps `grad`.
import "std:fmt"
import "std:sys"
import "std:zeus"

fn main() {
    zeus.snapshot(80, 40, fn() -> Node {
        zeus.set_background_end(zeus.Box(width = 80, height = 40, background = "#ff0000"), 255)
    })
    sys.exit(0)
}
