import "std:zeus"
import "std:router"

fn page(p: Params) {
    zeus.Text("home")
}

fn meta(p: Params) -> Meta {
    return Meta {
        title: "Home",
        description: "The routes fixture home page.",
        og_image: "/og/home.png",
    }
}
