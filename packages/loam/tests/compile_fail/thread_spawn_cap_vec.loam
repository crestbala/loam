import "std:thread"
import "std:fmt"

// A []int is Copy (refcounted handle) but not Send: sharing the buffer with a
// worker would race the non-atomic refcount. Must be rejected at spawn.

fn main() {
    let xs = []int { 1, 2, 3 }
    thread.spawn(|| {
        fmt.println("sum", xs[0])
    })
}
